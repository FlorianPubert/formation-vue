import { Robot } from '@/models/Robot';
import { createStore } from 'vuex';

export default createStore({
  state: {
    cart: Array<Robot>(),
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});
